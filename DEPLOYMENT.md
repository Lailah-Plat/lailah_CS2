# دليل تشغيل ونشر تطبيق "ليلة" (Laylah)

هذا المستند يشرح كيفية تشغيل التطبيق على جهاز الكمبيوتر الخاص بك أو نشره على منصات استضافة خارجية.

## أولاً: التشغيل المحلي (Local Development)

### المتطلبات الأساسية
*   تثبيت **Node.js** (الإصدار 18 أو أحدث).
*   مشغل حزم (مثل **npm** أو **yarn**).

### خطوات التشغيل
1.  قم بتحميل كود المشروع (ZIP) أو عمل `git clone` للمستودع.
2.  افتح Terminal في مجلد المشروع.
3.  قم بتثبيت المكتبات البرمجية:
    ```bash
    npm install
    ```
4.  قم بتشغيل التطبيق في وضع التطوير:
    ```bash
    npm run dev
    ```
5.  افتح المتصفح على الرابط: `http://localhost:3000`

---

## ثانياً: خيارات النشر والاستضافة (Deployment)

### 1. المنصات السحابية السريعة (Vercel / Netlify)
هذه هي أسهل طريقة لنشر التطبيق (Frontend):
*   اربط مستودع GitHub الخاص بك بالمنصة.
*   الإعدادات الافتراضية ستكون:
    *   **Build Command:** `npm run build`
    *   **Output Directory:** `dist`

### 2. الاستضافة بوضعية Full-Stack (Server-side)
بما أن التطبيق يستخدم Express كخلفية (Backend) لبعض العمليات، يمكنك استخدامه كـ Full-stack:
*   تأكد من إعداد متغيرات البيئة (Environment Variables) مثل مفتاح Gemini API إذا كنت تستخدم ميزات الذكاء الاصطناعي.
*   أمر البناء: `npm run build`
*   أمر التشغيل: `npm start`

---

## ثالثاً: تصدير المشروع من AI Studio
للحصول على نسخة من الكود:
1.  من واجهة **Google AI Studio Build**.
2.  اضغط على أيقونة الإعدادات أو زر "Export".
3.  اختر **Download ZIP** للحصول على الملفات فوراً.
